# 剧本创作页面逻辑盘点与补充清单

> 更新日期：2026-08-11
>
> 盘点依据：当前原生代码、Task 5–10 实施与验证报告
>
> 用途：页面验收、缺口识别和后续迭代排期

## 1. 统一口径

`/script-gen` 是四入口启动台和最近项目列表；`/script-gen/:projectId/workspace` 是生产原生项目工作台。静态验收演示页只保存当前页状态，不连接生产后端，非生产持久化页面。

八阶段固定为：创作设置 → 小说上传 → 小说分析 → 改编方案 → 结构化文字剧本 → 剧本正文 → 审核修订 → 文字分镜。场景资产不是第九阶段，其四层数据为：场景母资产 → 场景变体 → 剧本场景实例 → 分镜场景快照。

## 2. 页面完整性判断

当前原生工作台的八阶段页面、场景资产库/详情、任务进度、条件引导、生成结果和画布交接已完成装配。下一阶段工作的重点不是继续增加顶栏按钮，而是逐页验证后端适配、Obsidian 实际落盘和所有异常态。静态演示具备场景资产交互样例，但仅用于确认设计。

| 优先级 | 判断 | 原因 | 行动 |
|---|---|---|---|
| P0 | 补齐生产生成适配 | 部分阶段没有安全的模型感知后端合同时会明确失败 | 为每个生成动作提供模型、积分、候选版本与错误码合同 |
| P0 | 完成 Obsidian 服务端落盘 | 当前有 Markdown 投影与文档规范，不能等同于全部文件已真实写入 | 建立幂等文件服务、索引、影响记录与失败恢复 |
| P0 | 全量异常态验收 | 正常链路存在不代表归档、权限、冲突和重试均可用 | 按第 4 章逐页执行 |
| P1 | 场景历史列表体验 | 后端可按版本恢复，原生详情的历史版本列表仍可加强 | 增加受权限保护的版本列表接口与分页 |

## 3. 页面与交互清单

| 页面/区域 | 已有核心能力 | 必须验证/补充 | 完成标准 |
|---|---|---|---|
| 四入口启动台 | 快速、专业、小说上传、TVC；最近项目 | 空列表、加载失败、旧项目恢复阶段 | 四入口均进入 `/script-gen/new`，继续项目进入原生工作台 |
| 新建项目 | 创建类型与创作设置 | 参数持久化失败不得跳转 | 项目和参数版本都成功后进入工作台 |
| 工作台框架 | 八阶段 rail、页脚确认、自动保存 | 项目切换竞态、刷新恢复、合法回访 | 旧项目异步响应不污染新项目 |
| 创作设置 | 类型、题材、语气、受众、集数/时长、模型与积分 | 3001 无模型/不可达、真实模型无报价 | 仅显式演示模型为 0 积分 |
| 小说上传 | 文件上传与粘贴文本 | 2000 汉字边界、超限引导、解析失败 | 正好 2000 可保存，超限保持按钮可点并引导文件上传 |
| 小说分析 | 梗概、事件、章节大纲、世界观、人物详情 | 编辑梗概、新增事件、人物详情、编辑世界观、地点转资产 | 后端返回更新版本后才改本地状态 |
| 改编方案 | 高压开场、规则、确认、重生成 | 开场持久化证据、确认后不可改、候选采纳/放弃 | 前置不足零写入，采纳服务端优先 |
| 结构化文字剧本 | 单集结构、节拍、局部/整阶段重生成 | 稳定 episode/beat ID、无集数据 | 打开单集结构、新增节拍、重新生成节拍均可回访 |
| 剧本正文 | 场景/正文块、AI 局部动作、检查、导出 | 未选块、场景资产未决、导出错误 | 新增场景/正文块与候选结果不静默丢失 |
| 审核修订 | 筛选、局部修订、对比、通过 | 跨场景 HIGH/BLOCKER、真实定位写回 | 阻断列表不被旧筛选隐藏 |
| 文字分镜 | 新增/拆分/合并、视图切换、连续性、锁定、导出、画布 | 绑定冲突、快照缺失、fingerprint、归档 | 每镜头使用服务端锁定快照后才可归档/交接 |
| 场景资产库 | 检索、筛选、创建、详情、版本、变体、引用影响 | 缓存降级、DISABLED、ARCHIVED、搜索边界 | 降级只读；非 ACTIVE 不能新增绑定 |
| 场景资产详情 | 基本信息、版本、变体、引用、影响、Markdown | 历史版本列表与恢复、影响刷新失败 | 已持久化变更与影响刷新失败分别表达 |
| 任务/进度 | API pending/processing/completed/failed/cancelled；UI queued/running 映射、取消、轮询 | 超时、本地中止、刷新恢复 | completed 前不发布产物 |
| 生成结果 | 差异、实际积分、采纳、放弃 | 双击、相反决定并发、基线冲突 | server-first，candidate 隔离且幂等 |
| 阶段过渡 | 0–100% 百分比、返回、失败重试 | 99% 等待真实成功、最终阶段完成 | 成功持久化前不进入下一阶段 |

## 4. 非正常状态覆盖

每个有数据请求的页面至少要有加载态、空状态、错误态和重试；每个写操作还要覆盖权限不足、版本冲突和重复提交。项目 `ARCHIVED` 时写操作只读；场景资产 `DISABLED` 或 `ARCHIVED` 时不能新增引用；过期消费者展示 `STALE`，锁定快照展示 `PINNED`。

| 状态 | 页面表现 | 允许操作 | 禁止行为 |
|---|---|---|---|
| 加载态 | 骨架/进度、明确加载对象 | 取消或返回 | 展示旧项目数据为当前结果 |
| 空状态 | 说明缺少的上游与价值 | 直达补救动作 | 空白屏或无响应按钮 |
| 错误态 | 错误码、用户提示、request/task ID | 重试、返回、保留草稿 | 合成成功 Toast |
| 权限不足 | 说明 VIEW/EDIT_CONTENT 等需要的权限 | 返回项目或申请权限 | 仅前端隐藏而不做后端鉴权 |
| 网络中断 | 保留本地未提交内容 | 恢复后重试 | 丢弃编辑或改变当前版本 |
| 版本冲突 | 展示基线与当前版本 | 对比、刷新、重新提交 | 覆盖他人/新版本 |
| 项目 ARCHIVED | 只读标识 | 查看、导出既有结果、按权限恢复 | 新建任务、版本、绑定或画布项目 |
| 资产 DISABLED | 可查看历史与影响 | 激活或选择其他资产 | 新增场景引用 |
| 资产 ARCHIVED | 只读标识 | 查看历史 | 激活、编辑、新增引用 |
| 消费者 STALE | 黄色影响提示 | 查看差异、升级新版、保留旧版 | 自动覆盖 |
| 快照 PINNED | 锁定版本与 fingerprint | 查看/交接 | 随母资产更新而改写 |
| 缓存降级 | 标记数据可能过期 | 浏览、刷新 | 用缓存列表创建新绑定 |
| 任务 failed/cancelled | 保留旧产物、失败原因 | 新 task ID 重试 | 重复扣费或伪 completed |

## 5. 场景资产跨阶段逻辑

1. 小说分析保存世界观与地点为同一新版本，之后才能“转为场景资产”。
2. 转换生成场景母资产，可继续创建场景变体；重复转换打开已有资产详情。
3. 剧本正文的剧本场景实例选择母资产及可选变体，并固定双方版本；用户也可以明确“暂不绑定”，但进入分镜前持续提示。
4. 文字分镜按镜头保存引用并在锁定时由后端形成分镜场景快照。
5. 母资产语义更新后，未锁实例变为 `STALE`；锁定或 superseded 分镜仍为 `PINNED`；管理字段变更保持 `CURRENT`。

Obsidian 固定包含 `04-场景资产/00-场景资产索引.md`，并以 stable ID、immutable version、variant 和 consumer 双链关联正文与分镜。下游重生成不得破坏旧链接。

## 6. 模型、积分与候选检查：当前实现与目标合同

- **当前实现**：`/api/v1/ai/models` + 本地模型注册表提供模型与演示兜底，`/api/v1/credits/estimate`、任务预计/实际积分是兼容估算，只可用于 UI 守卫，不是 3001 账务证据。
- **目标合同（P0 延期）**：读取 3001 权威模型目录与价格；只有显式演示模型允许 0 积分；真实任务完成预冻结/预消费、实际结算、失败/取消退款或退回差额，幂等请求不重复扣费。
- API 状态是 `pending → processing → completed | failed | cancelled`；UI 可显示为 `queued → running → completed | failed | cancelled`，不得把 UI 文案当后端枚举。
- completed 生成任务只形成 candidate；采纳成为 accepted 并切换当前版本，放弃成为 discarded 且不改变正文。
- candidate/discarded 不得进入公开版本、恢复或下游上下文；实际积分、结果版本和错误码必须来自服务端。

“完成并归档”当前只调用 storyboard lock 并标记本地八阶段完成，不会归档 content project；项目 `ARCHIVED` 仍是独立目标能力/仓库动作。

## 7. 验收记录模板

| 页面 | 操作 | 前置条件 | 可见结果 | 后端/版本结果 | Markdown | 积分 | 异常路径 | 结果 |
|---|---|---|---|---|---|---|---|---|
| 示例：小说分析 | 编辑世界观 | 已加载 V3 | 保存中→成功 | 返回 V4 | 世界观及影响记录 | 0 | 409 保留草稿 | 待验收 |

静态验收演示可以证明控件、引导和当前页状态变化，不得在“后端/版本结果”栏填生产通过。生产验收必须以 API 响应、数据库版本、Markdown 投影/落盘与自动化测试为证据。
