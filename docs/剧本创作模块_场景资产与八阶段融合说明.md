# 剧本创作模块：场景资产与八阶段融合说明

> 状态：按当前代码同步的开发说明
>
> 更新日期：2026-08-11
>
> 事实源：原生前后端实现、数据库迁移 V1–V17、Task 5–10 自动化验证报告

## 1. 结论与边界

剧本创作保持一个项目、八个创作阶段，场景资产作为项目级共享能力接入小说分析、剧本正文和文字分镜，不增加第九阶段。权威顺序只有一套：创作设置 → 小说上传 → 小说分析 → 改编方案 → 结构化文字剧本 → 剧本正文 → 审核修订 → 文字分镜。

路由职责明确分开：`/script-gen` 是四入口启动台与最近项目列表；`/script-gen/new` 负责新建；`/script-gen/:projectId/workspace` 是具备后端持久化能力的生产原生工作台。静态验收演示 `eight-stage-workbench.html` 只保存当前页会话状态，用于交互确认，非生产事实源，不向后端、Obsidian 文件系统或画布项目写入数据。

场景数据固定划分为四层：场景母资产、场景变体、剧本场景实例、分镜场景快照。前两层用于复用，第三层记录某一剧本场景对具体资产版本的引用，第四层是在分镜锁定时形成的不可变快照。

## 2. 路由与组件图

| 路由/入口 | 原生组件 | 职责 | 当前状态 |
|---|---|---|---|
| `/script-gen` | `ScriptCreationHome.vue` | 快速创作、专业创作、上传小说、TVC 四入口及最近项目 | 生产原生 |
| `/script-gen/new` | `ContentProjectCreate.vue` | 创建项目并持久化创作设置 | 生产原生 |
| `/script-gen/:projectId/workspace` | `ContentProjectWorkspace.vue` | 八阶段、模型/积分、任务结果、场景资产与画布交接 | 生产原生 |
| 工作台左轨 | `WorkflowRail.vue` | 完成/当前/待处理状态与合法回访 | 生产原生 |
| 阶段 1–8 | `stages/*Stage.vue` | 八阶段业务操作 | 生产原生 |
| 项目场景资产 | `SceneAssetLibrary.vue`、`SceneAssetDrawer.vue`、`SceneAssetPicker.vue` | 检索、版本、变体、影响、绑定 | 生产原生 |
| 统一反馈 | `ActionGuidanceDialog.vue`、`GenerationProgressDialog.vue`、`ActionResultDrawer.vue` | 前置引导、百分比、采纳/放弃 | 生产原生 |
| 静态演示页 | `eight-stage-workbench.html` | 不连后端的验收样例 | 静态演示，当前页有效 |

`ContentProjectWorkspace.vue` 是组合根。项目切换时必须清空上一个项目的阶段、场景资产、任务、结果、产物和积分状态；所有异步响应以项目 ID 与操作代次校验，过期响应不得回写。

## 3. 八阶段输入、处理与产物

| 阶段 | 主要输入 | 处理与退出条件 | Markdown 产物 |
|---|---|---|---|
| 创作设置 | 类型、题材、语气、受众、集数/时长、模型 | 必填配置持久化；真实模型需正积分估算 | `01-创作设置.md` |
| 小说上传 | 文件或粘贴文本 | 粘贴最多 2000 个汉字；确认已保存原文 | `02-小说原文/00-原文索引.md`、章节文件 |
| 小说分析 | 已确认原文 | 保存梗概、事件、章节大纲、世界观与人物；地点可转场景资产 | `03-小说分析/*` |
| 改编方案 | 分析版本、创作设置 | 高压开场已持久化、至少一条规则并确认 | `04-改编方案.md` |
| 结构化文字剧本 | 已确认改编方案 | 单集结构与稳定节拍 ID 已保存 | `05-分集结构/EP-NNN-单集结构.md` |
| 剧本正文 | 单集结构、场景资产 | 场景/正文块已保存；每个场景明确绑定或暂不绑定 | `06-剧本正文/EP-NNN-剧本正文.md` |
| 审核修订 | 正文当前公开版本 | HIGH/BLOCKER 已解决或豁免并审核通过 | `07-审核修订/EP-NNN-审核记录.md` |
| 文字分镜 | 已审核正文、剧本场景实例 | 连续性检查通过；镜头锁定不可变快照 | `08-文字分镜/EP-NNN-文字分镜.md` |

阶段底部操作保持可点击。前置不足时返回结构化引导且零写入；持久化成功后才改变本地阶段和进度。阶段 1–7 只允许进入紧邻下一阶段；文字分镜持久化完成后总进度才从 88% 到 100%。

## 4. 场景四层与版本规则

| 层级 | 稳定身份 | 版本/状态 | 修改规则 |
|---|---|---|---|
| 场景母资产 | `asset_id` + `stable_id` | 生命周期 `ACTIVE` / `DISABLED` / `ARCHIVED`；语义版本 immutable | 名称/标签等管理变更不使消费者过期；空间、视觉语义等变更新建版本 |
| 场景变体 | `variant_id`，归属一个母资产 | 变体版本单调递增；配对可选但 ID/version 必须同时存在 | 昼夜、天气、损毁、节日等差异独立维护，不递增母资产版本 |
| 剧本场景实例 | 稳定 `SCRIPT_SCENE` target key | 固定引用母资产版本及可选变体版本；同步态 `CURRENT` / `STALE` / `PINNED` | 更新新版或保留旧版均需显式决策；更换空间需选择“仅当前场景”或“更新母资产” |
| 分镜场景快照 | shot/version 绑定及 fingerprint | 锁定后 immutable；锁定或 superseded 版本保持 `PINNED` | 母资产后续修改不回写快照；合并镜头要求场景与绑定完全一致 |

新增绑定只能使用 `ACTIVE` 资产。`DISABLED` 可查看但不能新增引用；`ARCHIVED` 只读且不能恢复为新绑定。归档前必须读取权威影响，存在活动引用时返回冲突并引导先替换引用。恢复历史版本会生成新的不可变版本，不覆盖历史。

语义变更后，未锁定且仍引用旧版的消费者标记 `STALE`；引用当前语义版的消费者为 `CURRENT`；已锁定分镜快照始终为 `PINNED`。替换迁移按消费者逐项执行并保留操作结果，不能用一次前端批量改写伪造成功。

## 5. 前后端数据契约

前端内部统一 camelCase，HTTP 边界统一 snake_case；项目工作区资产应用显式携带 `X-Workspace-Id: project_<projectId>`，全局拦截器不得覆盖。关键引用结构如下：

```json
{
  "scene_id": "SCRIPT-SCENE-001",
  "scene_asset_id": 101,
  "scene_asset_version_id": 1004,
  "scene_variant_id": "SCENE-VARIANT-001",
  "scene_variant_version_id": 3,
  "sync_status": "CURRENT"
}
```

锁定快照由服务端生成 fingerprint，前端不能自行制造：

```json
{
  "shot_id": 9001,
  "scene_asset_id": 101,
  "scene_asset_version_id": 1004,
  "scene_variant_id": "SCENE-VARIANT-001",
  "scene_variant_version_id": 3,
  "scene_snapshot_fingerprint": "sha256:...",
  "snapshot_locked": true
}
```

场景资产接口根路径为 `/api/v1/content-projects/{projectId}/scene-assets`：

| 方法与路径 | 用途 |
|---|---|
| `GET /api/v1/content-projects/{projectId}/scene-assets` | 按关键词、空间、复用、生命周期、引用筛选 |
| `POST /api/v1/content-projects/{projectId}/scene-assets` | 创建场景母资产 |
| `POST .../scene-assets/from-location` | 从已保存世界观地点转换 |
| `GET/PATCH .../scene-assets/{assetId}` | 详情与新语义版本 |
| `POST .../{assetId}/variants`、`PATCH .../{assetId}/variants/{variantId}` | 创建/更新场景变体 |
| `GET .../{assetId}/markdown` | 获取安全、确定性的 Markdown 投影 |
| `POST .../{assetId}/versions/{versionId}/restore` | 从历史版本生成新版本 |
| `POST .../{assetId}/activate` | 激活资产 |
| `POST .../{assetId}/disable` | 停用资产 |
| `POST .../{assetId}/archive` | 归档资产（存在活动引用时阻断） |
| `GET .../{assetId}/impact` | 获取权威消费者及 CURRENT/STALE/PINNED 状态 |

生成任务使用 `/api/v1/generation-jobs`，通过 `Idempotency-Key` 防止重复创建；读取、取消、采纳、放弃分别为 `GET /{jobId}`、`POST /{jobId}/cancel`、`POST /{jobId}/accept`、`POST /{jobId}/discard`。例如：`POST /api/v1/generation-jobs/{jobId}/accept`。

## 6. 生成候选、模型与积分

### 6.1 模型选择

原生工作台优先通过现有 BFF `/api/v1/ai/models` 获取 3001 真实可用模型。只有接口不可达或没有可用模型时才展示内置演示模型；只有用户显式选择演示模型才允许 `0 积分`。真实模型缺少有限且大于零的积分预估时返回 `POINT_ESTIMATE_REQUIRED`，不创建任务。

### 6.2 候选生命周期

生成任务状态为 `queued` → `running` → `completed`，异常终态为 `failed` 或 `cancelled`。`completed` 只表示服务端已生成隔离的 `candidate` 内容版本，不自动切换当前版本。返回结果至少包含 `result_version_id`、`actual_credits`、产物引用、差异、错误码和候选处置状态。

采纳/放弃遵循服务端优先：先调用 accept/discard，成功后再更新前端结果。采纳把候选置为 `accepted` 并以生成时基线执行 CAS 切换当前版本；放弃置为 `discarded` 且不改变当前内容。候选隔离要求 candidate/discarded 不进入公开版本列表、恢复或下游生成上下文。同一决定幂等，采纳与放弃并发返回 `IN_FLIGHT_CONFLICT`；基线已变化返回编辑冲突，不能覆盖新内容。

### 6.3 积分闭环

真实请求完整沿用 3001 现有积分规则：预估 → 预冻结/预消费 → 依据实际 Token 与倍率实际结算 → 退回差额或按规则补差。任务、结果、版本、积分流水与审计记录共享 task ID；重试创建新任务并关联原任务。Idempotency-Key、服务端终态与结算单共同保证不重复扣费。失败/取消按 3001 规则释放未消费积分；若取消未赢得竞态且候选已成功落库，则按成功任务结算。

## 7. Obsidian Vault 与变更传播

```text
项目名/
├── 00-项目主页.md
├── 01-创作设置.md
├── 02-小说原文/
├── 03-小说分析/
├── 04-场景资产/
│   ├── 00-场景资产索引.md
│   └── SCENE-ASSET-001-林野出租屋.md
├── 04-改编方案.md
├── 05-分集结构/
├── 06-剧本正文/
├── 07-审核修订/
├── 08-文字分镜/
├── 90-变更影响.md
└── 99-生成与计费记录.md
```

兼容已有 Vault 时允许保留原阶段目录编号，但稳定 `artifact_id`、版本 ID、场景/变体/消费者双链必须保持不变；导出服务通过清单映射目录，而不能仅凭目录数字推断业务阶段。场景索引固定路径是 `04-场景资产/00-场景资产索引.md`。

| 变更源 | 直接失效 | 保持不变 | 用户决策 |
|---|---|---|---|
| 创作设置 | 阶段 2–8 当前产物 | 历史版本 | 查看影响、重生成或保留 |
| 小说世界观地点 | 对应场景母资产转换提示及下游引用 | 已锁分镜快照 | 更新资产或保留旧地点 |
| 场景母资产语义版本 | 未锁剧本场景实例 `STALE` | 锁定快照 `PINNED` | 查看差异、升级新版、保留旧版 |
| 场景变体语义版本 | 引用该变体的未锁消费者 | 其他变体与锁定快照 | 升级或保留 |
| 剧本正文 | 审核修订、文字分镜 | 场景母资产 | 重审与重生成 |
| 已审核正文 | 文字分镜 | 旧分镜版本 | 重生成或继续旧版 |

每次改变当前 Markdown 都创建新版本并更新稳定链接；`90-变更影响.md` 记录 stale 来源、受影响消费者和决策；AI 任务另写 `99-生成与计费记录.md`。不保存 API Key、令牌或 3001 管理密钥。

## 8. 可见操作追溯矩阵

下表中的“失败结果”均指结构化 Action Guard/任务结果，条件不足时不创建任务、不改版本、不计费。

| 阶段 | 操作 | 前置条件 | 成功结果 | 失败结果 | Markdown 产物 | 失效下游 | API | 验收测试 |
|---|---|---|---|---|---|---|---|---|
| 小说分析 | 编辑梗概 / 新增事件 / 人物详情 / 编辑世界观 | 已加载分析版本；保存返回更新版本 | 新分析版本及可回访结果 | 校验、权限或持久化引导 | `03-小说分析/*` | 改编方案至文字分镜 | 内容单元草稿/版本接口 | upstream contract：版本递增、失败零写入 |
| 小说分析 | 地点转场景资产 | 世界观与地点同一已保存版本 | 场景母资产与稳定 location 引用 | `WORLD_LOCATION_REQUIRED` 或重复资产详情 | `04-场景资产/*.md` | 新建引用不使旧产物失效 | `POST .../scene-assets/from-location` | lifecycle E2E + upstream contract |
| 改编方案 | 选择高压开场 / 新增改编规则 / 确认改编方案 | 开场已持久化、规则完整、设置完整 | 确认版本 | `HOOK_PERSISTENCE_REQUIRED` 等引导 | `04-改编方案.md` | 结构化文字剧本至文字分镜 | 内容草稿/阶段持久化 | upstream contract |
| 改编方案 | 重新生成当前产物 | 当前产物已保存、模型与积分有效 | completed candidate 与差异 | 任务 failed/cancelled 或前置引导 | 候选采纳后更新方案 | 下游 `STALE` | `POST /api/v1/generation-jobs` | model + routing contract |
| 结构化文字剧本 | 打开单集结构 / 新增节拍 | 存在可编辑集 | 打开或新增稳定 beat ID | `EPISODE_NOT_AVAILABLE`/持久化失败 | `05-分集结构/EP-*.md` | 正文至分镜 | 内容单元草稿接口 | downstream contract |
| 结构化文字剧本 | 重新生成节拍 / 重新生成当前产物 | 节拍或阶段产物存在 | 隔离候选，采纳后新版本 | 前置引导或任务终态错误 | 对应单集结构 | 正文至分镜 | generation jobs + accept/discard | candidate lifecycle tests |
| 剧本正文 | 续写选中段落 / 增强冲突 / 精简对白 / 改写语气 / 检查角色一致性 | 选中稳定正文块 | 差异候选，采纳后正文版本 | `SCRIPT_BLOCK_REQUIRED`/任务失败 | `06-剧本正文/EP-*.md` | 审核、分镜 `STALE` | generation jobs | downstream + candidate tests |
| 剧本正文 | 新增场景 / 新增正文块 | 场景/正文结构有效 | 持久化结构与稳定 ID | 校验或保存失败 | 对应正文文件 | 审核、分镜 | 内容草稿接口 | downstream contract |
| 剧本正文 | 运行正文检查 / 导出正文 / 重新生成当前产物 | 有正文公开版本 | 检查结果、导出任务或候选 | 无内容、权限、任务错误 | 正文/检查/导出记录 | 采纳重生成后审核、分镜 | 检查/导出/generation adapter | downstream + routing tests |
| 剧本正文 | 场景资产绑定 | ACTIVE 母资产；变体 ID/version 成对 | 剧本场景实例固定版本 | DISABLED/ARCHIVED/缓存降级引导 | 正文中的稳定资产链接 | 新绑定 `CURRENT` | 资产应用接口 | scene asset UI contract |
| 审核修订 | 筛选问题 / 保存局部修订 / 对比修订前后 | 问题定位及真实 sceneId/blockId | 筛选、双版本修订与差异 | 定位失败零写入 | 审核记录与正文版本 | 分镜 `STALE` | 审核/内容草稿接口 | downstream contract |
| 审核修订 | 审核通过本集 / 重新生成当前产物 | 无未解决 HIGH/BLOCKER；产物已保存 | 通过版本或隔离候选 | `REVIEW_BLOCKED`/任务错误 | `07-审核修订/EP-*.md` | 文字分镜 | 审核/generation adapter | cross-scene blocker tests |
| 文字分镜 | 新增镜头 / 拆分镜头 / 合并镜头 | 稳定 scene/shot ID；合并绑定完全一致 | 服务端持久化镜头 | `SHOT_REQUIRED`/绑定冲突 | `08-文字分镜/EP-*.md` | 当前分镜版本 | plural storyboard APIs | snapshot E2E + downstream contract |
| 文字分镜 | 切换卡片/表格 | 无写入前置 | 本地视图切换 | 无 | 无 | 无 | 无服务端 API | DOM/UI contract |
| 文字分镜 | 连续性检查 | 有镜头与场景绑定 | 按场景/变体/道具/角色/轴线分组 | 检查服务失败可重试 | 分镜检查记录 | 阻断锁定 | storyboard continuity | snapshot E2E |
| 文字分镜 | 完成并归档 | 检查通过、每镜头有 fingerprint 锁定快照 | 锁定并归档；快照 `PINNED` | 快照缺失/归档冲突 | 分镜与项目主页 | 无，快照冻结 | storyboard lock + project archive | snapshot E2E |
| 文字分镜 | 配置导图 / 创建画布项目 | 已锁正文、分镜与场景快照 | 导出结果或画布快照任务 | 权限/锁定前置引导 | 导出/交接记录 | 无 | export + canvas snapshot | routing/downstream tests |
| 文字分镜 | 重新生成当前产物 | 当前分镜已保存 | 隔离候选；采纳后新版本 | 模型/积分/任务错误 | 分镜文件 | 旧锁定快照不变 | generation jobs | candidate lifecycle tests |

## 9. 页面状态清单

启动台、项目创建、原生工作台、场景资产库/详情、任务进度、生成结果、版本/影响与画布交接均要覆盖：首次空状态、加载态、正常态、网络/服务错误态、权限不足、重试中、项目 `ARCHIVED` 只读、资产 `DISABLED`/`ARCHIVED`、消费者 `STALE`/`PINNED`、任务 `failed`/`cancelled`、候选冲突和页面刷新恢复。缓存降级只允许浏览，不能据此创建新绑定。

## 10. 数据库迁移至 V17

部署顺序必须执行完整 Flyway 链。与本融合直接相关的是：

- `V16__scene_asset_storyboard_snapshots.sql`：补充分镜镜头的场景资产/变体版本引用、锁定状态与 fingerprint，并建立快照约束。
- `V17__asset_application_target_key.sql`：为 `asset_applications` 增加稳定 `target_key` 及 `(target_type, target_key)` 索引，使 `SCRIPT_SCENE` 不再依赖不稳定数字 ID。

V17 是当前代码事实，不得写成“止于 V16”。MySQL 8 采用普通 `ADD COLUMN` 与 `CREATE INDEX`，依赖 Flyway 的单次版本迁移语义；H2 测试库和 MySQL 基线 schema 同步包含列与索引。

## 11. 错误码与恢复

| 错误码 | 说明 | 恢复动作 |
|---|---|---|
| `MODEL_REQUIRED` | 未选模型 | 返回创作设置选择模型 |
| `POINT_ESTIMATE_REQUIRED` | 真实模型缺少正积分预估 | 刷新模型计价或更换模型 |
| `HOOK_PERSISTENCE_REQUIRED` | 高压开场未持久化 | 保存开场后重试确认 |
| `SCRIPT_BLOCK_REQUIRED` / `SHOT_REQUIRED` | 未选择稳定正文块/镜头 | 聚焦可选项后重试 |
| `REVIEW_BLOCKED` | 存在 HIGH/BLOCKER | 展示跨场景权威阻断列表 |
| `SCENE_ASSET_NOT_ACTIVE` | 资产 DISABLED/ARCHIVED | 激活或更换资产 |
| `SCENE_BINDING_CURRENT` | 消费者已是当前版本 | 无需重复刷新 |
| `GENERATION_BASELINE_REQUIRED` | 旧候选缺少可验证生成基线 | 重新生成 |
| `IN_FLIGHT_CONFLICT` | 采纳/放弃并发 | 等待当前决定并刷新 |
| `VERSION_CONFLICT` | 当前版本已变化 | 对比并重新提交 |

所有失败保留原产物和候选结果（安全清理要求删除的未提交候选除外），返回 request/task ID 与可执行恢复动作；不以成功 Toast 代替服务端结果。

## 12. 部署、验证与回滚

部署顺序：备份数据库 → 执行 Flyway 至 V17 → 部署后端 → 验证场景/候选 API 与权限 → 部署前端 → 抽样检查 Obsidian 投影 → 开放入口。验收至少运行场景生命周期 E2E、分镜快照 E2E、候选决策 E2E、Task 4–9 前端合同、Vite 构建和静态原型测试。

回滚先关闭新写入口并等待运行任务终态，再回滚前端和后端。若 V17 已产生稳定 `target_key` 数据，优先保留兼容列，不直接执行破坏性 undo；只有确认旧版本不读取该列、已备份且不存在仅以 target_key 定位的引用时，才按 `V17_undo.sql` 回退。V16 快照列承载锁定证据，存在锁定分镜时不得删除；应用回滚应继续只读这些字段。积分结算和 accepted/discarded 历史不能随应用回滚删除。

## 13. 实施判断

当前生产原生工作台已具备八阶段装配、项目场景资产、V17 稳定脚本场景键、分镜锁定快照、候选隔离与服务端优先决策。仍需将“Obsidian 文件真实落盘”与各阶段尚无安全模型合同的生成适配按服务端能力逐项交付；适配缺失时页面必须返回明确失败，不能合成成功。静态演示仅用于交互验收，不应作为上述剩余能力已生产落地的证据。
