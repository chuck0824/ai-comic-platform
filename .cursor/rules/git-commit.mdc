---
description: Git 提交须按功能用简体中文说明
alwaysApply: true
---

# Git 提交规范

- 提交说明使用**简体中文**（标题必填；正文可选）。
- **按功能拆分**：每个 commit 只含一个功能点或修复点；文档、脚本、业务代码尽量分开提交。
- 标题：`类型: 简述`（feat / fix / chore / docs / refactor / test）。
- 正文写「为什么」，避免纯文件列表。
- 示例：`docs: 补充根目录 Cursor 与 AGENTS 规范`
- 禁止提交：密钥、`.env`、`one-api.db`、`.venv`、`.dev-logs/`、无关 lockfile 噪音（除非该功能确实依赖）。
